/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['imgcdn.stablediffusionweb.com', 'firebasestorage.googleapis.com'],
    },
  };
  
  export default nextConfig;
  